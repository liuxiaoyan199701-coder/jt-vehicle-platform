# Tasks

规则同前：只动 jt-terminal-simulator；只提交不推送不合并；SimulatorViewSmokeTest 与
全部既有回归必须保持绿。

- [x] 1.1 车队配置模型：模板 + 台数（≤20）+ 递增规则（设备号/手机号/车牌尾号）+
      出发错峰间隔；进持久化（@JsonCreator 兼容模式）
- [x] 1.2 多实例运行时：每台独立 SignalClient 生命周期（连接/注册/鉴权/上报/断开），
      共享调度线程池，单台故障不影响其它台
- [x] 1.3 行程错峰：同路线按台索引偏移起点与出发时间
- [x] 1.4 车队表格视图：每行状态/位置/速度/告警位实时刷新；启停全队与单台
- [x] 1.5 选中联动：表格选中 → 既有单车面板（告警/0702/拍照/推流）作用于选中台；
      推流仅允许对一台开启
- [x] 1.6 单测：递增规则边界（尾号进位）、错峰计算、单台断开不影响其它、
      旧配置（无车队字段）兼容加载
- [x] 1.7 全量测试绿 + strict 校验 + 如实勾选
- [x] 1.8 git commit（中文说明），不推送不合并
