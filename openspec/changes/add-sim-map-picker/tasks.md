# Tasks

规则同前：只动 jt-terminal-simulator；只提交不推送不合并。

- [x] 1.1 依赖：pom 加 javafx-web（版本与既有 javafx 模块一致，25.0.2）；
      选点页 HTML/Leaflet 打包进 resources（Leaflet 资源本地内嵌，不走 CDN）
- [x] 1.2 选点页：高德瓦片图层（wprd0{1-4}.is.autonavi.com 标准样式）、
      起点/终点双标记（点击放置、可拖动）、底部显示当前坐标
- [x] 1.3 Java↔JS 桥：确认时把两点 GCJ-02 坐标回传 Java；打开时把既有
      起终点注入地图并居中（无既有点则默认上海附近）
- [x] 1.4 行程区「地图选点」按钮 → 模态对话框 → 确认回填表单；
      WebView 初始化失败或无网络时提示「地图不可用，请手输」，表单照常可用
- [x] 1.5 单测：桥回传坐标精度（6 位小数不丢）、回填后配置持久化、
      旧配置兼容；WebView 相关逻辑抽薄壳使核心可测
- [x] 1.6 全量测试绿 + strict + 如实勾选 + git commit（中文说明）不推送
