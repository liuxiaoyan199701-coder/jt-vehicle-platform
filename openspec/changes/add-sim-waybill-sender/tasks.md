# Tasks

规则同前：只动 jt-terminal-simulator；T0701 语义以 org.yzh.protocol.t808.T0701 为准；
只提交不推送不合并。

- [x] 1.1 运单发送：文本内容 UTF-8 编码进 T0701（长度字段按类定义），手动发送按钮
      + 预置两个模板（JSON 货运单、纯文本）
- [x] 1.2 行程联动：可开关「行程开始自动上报运单」，默认关；配置持久化兼容照旧
- [x] 1.3 界面：运单区（模板选择/文本编辑/发送与最近发送状态）
- [x] 1.4 单测：编码回环（中文不乱码）、空内容拒发、旧配置兼容
- [x] 1.5 全量测试绿 + strict + 如实勾选 + git commit（中文说明）不推送
