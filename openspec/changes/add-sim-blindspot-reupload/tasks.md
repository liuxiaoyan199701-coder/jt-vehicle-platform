# Tasks

规则同前：只动 jt-terminal-simulator；T0704 类已在 org.yzh.protocol.t808，
语义以它为准（补报类型字段注意盲区补报取值）；只提交不推送不合并。

- [x] 1.1 盲区配置进 TripConfig（自动盲区段：起止进度百分比，可多段；@JsonCreator 兼容）
- [x] 1.2 行程引擎：盲区内缓存 T0200 不发送；驶出时组 T0704（盲区补报类型，
      时间升序）发出后恢复实时上报；缓存有上限（默认 200 点，超限丢最旧）
- [x] 1.3 手动触发：界面「进入盲区/离开盲区」按钮 + 状态与缓存点数显示
- [x] 1.4 车队兼容：盲区仅作用于当前选中台，其它台不受影响
- [x] 1.5 单测：缓存上限丢弃策略、补报包内点序与数量、离开盲区后实时上报恢复、
      旧配置兼容加载
- [x] 1.6 全量测试绿 + strict 校验 + 如实勾选 + git commit（中文说明）不推送
