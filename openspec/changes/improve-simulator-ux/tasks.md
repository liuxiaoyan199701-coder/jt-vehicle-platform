# Tasks

## 1. 界面骨架重构

- [x] 1.1（无前置依赖）`SimulatorView` 拆分区：左侧配置折叠区（复用 `ConfigurationPane`）、
      右侧 `TabPane`（行程/视频/拍照/告警模拟/驾驶员/日志）、顶部常驻状态条。
      拆的过程保持 `SimulatorViewSmokeTest` 全绿——它是这次重构的安全网
- [x] 1.2（依赖 1.1）状态条接线：连接状态、设备号、行程进行中与已行驶里程、推流状态、
      最近上报时间，随运行事件实时刷新（走既有 `RuntimeListener` 通道）
- [x] 1.3（依赖 1.1）深色主题：扩展现有 `simulator.css`，对照度自查 4.5:1；
      表单校验错误就地显示在字段旁（复用 `ConfigValidation`），不弹窗
- [x] 1.4（依赖 1.1–1.3）冒烟与截图自查：窗口 1280×800 与 1024×700 两档不溢出、不裁切

## 2. 告警模拟面板

- [x] 2.1（无前置依赖）核对 `org.yzh.protocol` 的 warnBit 位定义，选定常用位清单
      （紧急、超速、疲劳、GNSS 模块故障、GNSS 天线未接/短路，可再加 2–3 个常用位），
      名称与位号对照写成常量表，注明出处
- [x] 2.2（依赖 2.1）`SignalClient` 位置上报携带当前置位集合；置位/解除线程安全
      （UI 线程改、上报线程读）
- [x] 2.3（依赖 2.2、1.1）告警面板：每位一个开关 + 全部解除；超速位附带
      「速度抬到 X km/h」联动输入（默认 80）
- [x] 2.4（依赖 2.2）单测：置位后 warnBit 含该位、解除后不含、多位并存、联动提速生效

## 3. 驾驶员身份（0702）

- [x] 3.1（无前置依赖）读 `org.yzh.protocol.t808.T0702` 与标准，确认状态值与
      IC 卡读取结果的取值语义（**不要凭常识猜**），写进代码注释
- [x] 3.2（依赖 3.1）`SignalClient` 增加 0702 发送：插卡（含全部证件字段）、拔卡、
      读卡失败；时间用 `TerminalTime.ZONE`
- [x] 3.3（依赖 3.2、1.1）驾驶员页签：姓名/身份证号/从业资格证编码/发证机构/有效期表单
      （记住上次输入），三个动作按钮，发送结果反馈到状态区
- [x] 3.4（依赖 3.2）单测：0702 编码回环（编出的报文按协议解回字段一致）、
      读卡失败时结果字段非 0、时间字段为东八区

## 4. 1078 SIM 形态选择

- [x] 4.1（依赖 1.1）配置区加三形态下拉（标准 12 位 / 扩展 20 位·置X位 / 野生 20 位），
      默认标准；说明文案写明野生形态的用途
- [x] 4.2（依赖 4.1）选择进入 `SimulatorConfig` 持久化；推流链路把它传给
      `Jt1078PacketEncoder(simFormat)` 构造（协议层已就绪，纯接线）
- [x] 4.3（依赖 4.2）单测：三形态分别推一段，封包第 4 字节与 SIM 字节数正确
      （对照 `SimWidthRoundTripTest` 的构造方式）

## 5. 配置持久化兼容

- [x] 5.1（依赖 2.3、3.3、4.2）全部新增配置项进 JSON 持久化，照 `TripConfig` 的
      `@JsonCreator` 包装类型工厂模式——**Jackson 3 对原始类型收到 null 直接拒绝**，
      旧配置文件缺字段必须能加载
- [x] 5.2（依赖 5.1）兼容测试：删掉新增字段的旧配置文件正常加载并取默认值

## 6. 验证与交付

- [x] 6.1（依赖前述全部）`mvn -pl jt-terminal-simulator -am test` 全绿，
      特别确认 `SimulatorViewSmokeTest`、`PhotoSubpackageRoundTripTest` 未破
- [x] 6.2（依赖 6.1）`openspec validate improve-simulator-ux --strict` 通过；
      本清单全部勾选
- [ ] 6.3（依赖 6.1）手动端到端（连本地或测试平台）：置位疲劳驾驶 → 平台出告警；
      插卡 → 平台收到 0702；切野生 20 位推流 → 画面正常且媒体指标计数正确
      需人工执行，本轮未做
- [x] 6.4（依赖 6.3）git commit 到本分支（中文说明）；**不要推送、不要合并**——
      由规划侧 review 后统一处理
